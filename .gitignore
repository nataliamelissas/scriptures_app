# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# Editor config (local)
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Wrangler (Cloudflare Workers) local cache — contains account info
.wrangler/
workers/**/node_modules/

# Firebase platform config (contains project identifiers; safe but conventionally untracked)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist

# Claude/agent settings (local)
.claude/

# Flutter DevTools local state
devtools_options.yaml

# Local environment
.env

# Firebase config (contains project keys/IDs — regenerate via `flutterfire configure`)
lib/firebase_options.dart
firebase.json
